# Changesets

Run `bun run changeset` for a user-facing package change and commit the resulting Markdown file. The three published packages share a fixed version while their APIs evolve. Apps and examples remain private.

See [the release guide](../docs/releases.md) for the release PR, npm setup, and first publication.
