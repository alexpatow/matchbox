---
"@matchbox-ai/core": minor
"@matchbox-ai/train": minor
"matchbox-ai": minor
---

Support typed JSON inputs and explicit numeric feature classification. Add `NumericEncoder`, `InferInput`, and `featureClassifier`, preserve input types through generated wrappers and React, and accept JSON input in the CLI and workbench. Existing text classifiers and their APIs remain supported.
