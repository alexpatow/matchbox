# @matchbox-ai/train

Read [the bundled documentation](docs/README.md) for the installed version. Train exports explicit pipeline primitives and programmatic training. Install @matchbox-ai/cli for the matchbox command and browser workbench. Native TensorFlow loads for training; these modules must not enter browser bundles.
