{
  "name": "@matchbox-ai/train",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "matchbox": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "templates",
    "docs"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "bun ../../scripts/package-docs.ts train && rolldown --config && chmod +x dist/cli.js && tsc -p tsconfig.build.json",
    "dev": "bun run --parallel \"dev:*\"",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.tools.json",
    "dev:js": "rolldown --config --watch",
    "dev:types": "tsc -p tsconfig.build.json --watch"
  },
  "dependencies": {
    "@matchbox-ai/core": "workspace:*",
    "@tensorflow/tfjs-node": "4.22.0",
    "zod": "4.6.3"
  },
  "devDependencies": {
    "@types/node": "22.20.2",
    "rolldown": "1.2.8",
    "typescript": "7.0.2"
  }
}
