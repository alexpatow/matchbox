# Field classifier fixture

This fixture exercises finite-value classification, uncertainty, and TensorFlow export parity. It is test coverage for the public fieldClassifier primitive, not a recommended money parser. Run it with bun run matchbox-ai train tests/fixtures/field-classifier.
